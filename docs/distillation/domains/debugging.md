# Domain: debugging

| Skill | Tier | Primary bindings |
|-------|------|------------------|
| `root-cause-tracing` | T1 | host `wave-orchestration`; command `repair`; technique-hint |

Role:

1. Gate fixes behind a documented root cause.
2. Offer competing-hypothesis branching when initial traces disagree.

Notes:

- Absorbs `systematic-debugging`, `debugging-strategies`, `debug-buttercup`
  triage posture, and `parallel-debugging`.
- Narrowly bound but stays T1 because it expresses a reusable method.
