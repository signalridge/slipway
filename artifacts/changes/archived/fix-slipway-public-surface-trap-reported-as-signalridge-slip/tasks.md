# Tasks

## Task List

- [x] `t-01` Add a typed sentinel `ErrWavePlanCacheUnreadable` in the state package and wrap parse/validate failures of the persisted wave-plan cache in `loadWavePlanFromPath` so callers can distinguish a corrupt/unsupported-field cache from a tasks.md-derivation failure via `errors.Is`.
  - depends_on: []
  - target_files: [internal/state/wave_execution.go]
  - task_kind: code
  - covers: [REQ-002, REQ-004]

- [x] `t-02` In `loadAuthoritativeWaveExecution` (`cmd/common.go`), branch the wave-plan load error: keep `wave_plan_missing` for `fs.ErrNotExist`; for `errors.Is(err, state.ErrWavePlanCacheUnreadable)` emit `wave_plan_unreadable` with a remediation that names the engine-owned `wave-plan.yaml` cache, says it must not be hand-edited, and points at `slipway repair` then `slipway run`; reserve the existing "update tasks.md" remediation only for the remaining derive-from-tasks.md failure. Mirror the same `errors.Is(err, state.ErrWavePlanCacheUnreadable)` branch in the S2 health-report path (`internal/state/health.go`) so a corrupt engine-owned cache surfaces `wave_plan_unreadable` (cache-regenerate hint) instead of being misattributed to a tasks.md-derivation failure (`wave_plan_load_failed`).
  - depends_on: [t-01]
  - target_files: [cmd/common.go, internal/state/health.go, cmd/evidence.go]
  - task_kind: code
  - covers: [REQ-001, REQ-002]

- [x] `t-03` Align the `wave_plan_unreadable` and `wave_plan_load_failed` remediation/message wording so any surface that maps through recovery vocab names the engine-owned wave-plan cache and the never-hand-edit / regenerate guidance instead of implying a tasks.md authoring problem.
  - depends_on: []
  - target_files: [internal/model/recovery.go, internal/model/reason_code.go]
  - task_kind: code
  - covers: [REQ-001]

- [x] `t-04` Add a struct-level doc comment on `wavePlanView` (`cmd/next.go`) marking `input_context.wave_plan` as a diagnostic-only, non-persistable projection distinct from the persistable `wave-plan.yaml` schema (`model.WavePlan`), with view-only fields that must never be copied into the cache.
  - depends_on: []
  - target_files: [cmd/next.go]
  - task_kind: code
  - covers: [REQ-003]

- [x] `t-05` Document in `docs/commands.md` that `input_context.wave_plan` is a diagnostic-only projection and that `wave-plan.yaml` is an engine-owned cache regenerated by tooling (`slipway repair`) and never hand-edited. Also align the generated wave-orchestration host skill template (`internal/tmpl/templates/skills/wave-orchestration/SKILL.md.tmpl`) so it no longer says the persisted cache is "materialized from this projection": mark `input_context.wave_plan` diagnostic-only / non-persistable, state `wave-plan.yaml` is engine-owned and regenerated from authoritative `tasks.md`/engine state (not copied from the projection), and that view-only fields must never be round-tripped into the cache.
  - depends_on: []
  - target_files: [docs/commands.md, internal/tmpl/templates/skills/wave-orchestration/SKILL.md.tmpl]
  - task_kind: doc
  - covers: [REQ-003]

- [x] `t-06` Add tests: a state-level test that a wave-plan cache with an unsupported field fails closed and is wrapped by `ErrWavePlanCacheUnreadable`; a cmd-level test that the cache-unreadable path yields the engine-owned-cache remediation (names cache + regenerate, not tasks.md) while a tasks.md-derivation failure keeps tasks.md guidance; and assert view-only fields stay excluded from the persisted cache. Also lock the shared vocabulary in `internal/model/model_test.go` (so `wave_plan_load_failed` stays tasks.md-oriented while `wave_plan_unreadable` owns the engine-owned-cache / never-hand-edit story) and cover the S2 health-report path in `internal/state/health_test.go` (a corrupt cache surfaces `wave_plan_unreadable` + `slipway repair`, while an unschedulable tasks.md stays `wave_plan_load_failed`).
  - depends_on: [t-01, t-02, t-03]
  - target_files: [internal/state/wave_execution_test.go, cmd/common_test.go, cmd/next_wave_plan_test.go, internal/state/health_test.go, internal/model/model_test.go, cmd/evidence_task_test.go]
  - task_kind: test
  - covers: [REQ-001, REQ-002, REQ-003, REQ-004]
