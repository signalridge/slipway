# Active Workflow Feedback

## 2026-05-25T09:54:01Z

- Symptom: after advancing from S0 confirm to `S1_PLAN/research`, `next --json --diagnostics` reported `research.md` as draft in `artifact_status`, but the file did not exist until `run --json --diagnostics` was invoked and blocked on missing research evidence.
- Finding: `AdvanceGoverned` creates `research.md` during the S1 research `run` path; the read-only `next` projection can advertise the artifact before materializing it.
- Friction: research hosts are told to read `research.md`, but a strict `next`-only handoff may point to a not-yet-created artifact.
- Suggested fix: materialize the S1 research scaffold before surfacing the `research-orchestration` handoff, or mark the missing scaffold explicitly in the handoff action contract.
- Disposition: fixed by code. `advanceIntakeConfirm` now materializes `research.md` when a discovery change enters `S1_PLAN/research`; regression coverage is `TestAdvanceIntake_ConfirmToS1PlanResearchMaterializesResearchArtifact`.

## 2026-05-25T10:46:36Z

- Symptom: `slipway codebase-map` still does not automatically populate `artifacts/codebase/*.md` with repository facts; it creates/assesses the scaffold, but the actual mapping content remains absent unless an agent manually runs the `slipway-codebase-mapping` skill and writes the documents.
- Finding: this leaves a gap between the command name and the delivered behavior. The workflow can expose `input_context.codebase_map_dir` and `input_context.codebase_map_docs`, but the runtime does not appear to require or invoke the skill that is needed to turn those files into useful brownfield context.
- Friction: downstream planning/research/review hosts can see canonical codebase-map paths, yet the files may contain no substantive context. Even with scaffold-only detection, the operator still has to notice the missing skill execution and manually fill the map.
- Suggested fix: either make `slipway codebase-map` perform a deterministic baseline population itself, or make the governed handoff explicitly require/invoke `slipway-codebase-mapping` when the map is missing or scaffold-only. The important distinction is that scaffolding alone must not be treated as a completed codebase map.

- Symptom: after finalization/achieve, a new archive directory appeared at `artifacts/changes/archived/fix-slipway-governed-workflow-feedback-from-archived-clinvoker-end-to-end-run`, while the original reference archived feedback bundle still exists at `artifacts/changes/archived/reference-users-yixianlu-projects-clinvoker-to-add-ci-cd-release-and-maintenance-capabilities-while-exercising-the-full-slipway-governed-workflow`.
- Finding: the operator-facing result is ambiguous: it can look like the workflow achieved/archived into two directories, or like a follow-up remediation change archived separately from the original reference bundle.
- Friction: when a workflow run is itself about feedback on an archived bundle, the final archive layout does not clearly explain whether the new archive is an audit/remediation record, a relocation of the original bundle, or a duplicate terminal artifact.
- Suggested fix: when finalizing a change that was created to address feedback from an archived bundle, record an explicit relationship between the new archive and the source archive. `done`/`achieve` output should make clear that the new directory is a separate remediation archive, not a second archive of the original change, or should provide a single canonical pointer if the intent is consolidation.

## Archived Feedback Disposition Audit

Source: `artifacts/changes/archived/reference-users-yixianlu-projects-clinvoker-to-add-ci-cd-release-and-maintenance-capabilities-while-exercising-the-full-slipway-governed-workflow/workflow-feedback.md`

| Archived item | Disposition | Evidence |
|---|---|---|
| 2026-05-25T06:43:34Z S0 research handoff action says `complete research.md` while `next_skill=intake-clarification` | Fixed by code/tests. S0 research wording is now intake-scoped and does not request an S1 `research.md` artifact. | `internal/engine/governance/actions.go`, `internal/engine/governance/runtime_actions.go`, `cmd/progression_next_test.go` (`TestNextS0ResearchActionDoesNotRequestResearchMarkdown`) |
| 2026-05-25T06:46:53Z `codebase-map` creates placeholder-only docs but reports success | Fixed by code/tests. Codebase-map output and downstream stats distinguish `scaffold_only` from populated context. | `internal/engine/artifact/codebase_map.go`, `cmd/codebase_map.go`, `internal/state/stats.go`, `cmd/codebase_map_command_test.go`, `cmd/cli_e2e_test.go`, `internal/state/stats_test.go` |
| 2026-05-25T06:48:21Z research skill headings conflict with runtime schema | Fixed by guidance/tests. Research skill now names the schema-required top-level headings directly. | `internal/tmpl/templates/skills/research-orchestration/SKILL.md`, `internal/tmpl/templates_test.go` (`TestResearchOrchestrationUsesResearchArtifactSchemaHeadings`) |
| 2026-05-25T06:41:02Z structured verification `references` maps fail YAML parsing | Fixed by guidance/tests. Verification doctrine documents `references` as a YAML sequence of strings only. | `internal/tmpl/templates/_partials/verification-doctrine.tmpl`, `internal/tmpl/templates_test.go` (`TestVerificationDoctrineDocumentsStringOnlyReferences`) |
| 2026-05-25T07:02:04Z task parser rejects `evidence` and `acceptance` metadata | Fixed by code/tests. Wave task parsing accepts both keys and includes them in semantic hashes. | `internal/engine/wave/parse.go`, `internal/engine/wave/parse_test.go` |
| 2026-05-25T07:02:50Z S1 bundle has no plan-audit handoff in `next` | Fixed by code/tests. An unblocked `S1_PLAN/bundle` view now surfaces `next_skill.name=plan-audit` with an action warning. | `cmd/next_skill_view.go`, `cmd/progression_next_test.go` (`TestNextS1BundleSurfacesPlanAuditHandoff`) |
| 2026-05-25T07:16:43Z worktree-preflight evidence deadlocks behind missing worktree metadata | Already fixed with regression evidence and verified in this run. S2 preflight evidence is consumed before required-action blockers and this active change successfully bound/relocated into `.worktrees/fix-slipway-governed-workflow-feedback`. | `internal/engine/progression/advance_governed.go`, `cmd/worktree_preflight_test.go`, active `verification/worktree-preflight.yaml`, live S2 transition output |
| 2026-05-25T07:38:10Z bound worktree lacks exported `.codex/skills` host catalog | Fixed by documentation/guidance. The handoff contract now says a worktree code workspace is not necessarily the skill catalog root and callers should resolve `next_skill.name` through the source checkout or global installed catalog. | `docs/agent-contracts.md`, `internal/tmpl/templates/_partials/command-next-body.tmpl`, `internal/tmpl/templates_test.go` (`TestNextCommandDocumentsWorktreeSkillCatalogFallback`) |
| 2026-05-25T07:42:29Z parallel read-only `status` and `next` can hit `state_lock_timeout` | Fixed by documentation/policy. Agent contracts now declare same-change Slipway commands non-parallelizable unless a command explicitly documents shared-read behavior. | `docs/agent-contracts.md` |
| 2026-05-25T08:02:18Z full Go tests can time out at default 10 minutes | Fixed by documentation and active project context. Full-suite commands now use `go test -timeout=20m ./... -count=1`. | `docs/workflow-test-menu.md`, active `requirements.md`, active `tasks.md` |
| 2026-05-25T08:22:04Z S2 task acceptance required downstream review/closeout evidence | Fixed by guidance/tests. Plan-audit now treats future S3 review or S4 closeout evidence in S2 task acceptance as a planning blocker. | `internal/tmpl/templates/skills/plan-audit/SKILL.md`, `internal/tmpl/templates_test.go` (`TestPlanAuditBlocksFutureLifecycleAcceptanceCriteria`) |
| 2026-05-25T09:19:38Z root-scoped S1 bundle later relocates into worktree, creating two authoritative-looking paths | Deferred with rationale. Moving worktree binding before S1 bundle creation changes lifecycle ordering, path authority, and repair semantics. This batch keeps compatibility by consuming S2 preflight evidence, relocating the active bundle, and documenting canonical worktree handoff paths; early binding should be designed as a separate lifecycle change. | Deferred follow-up; current mitigation covered by `cmd/worktree_preflight_test.go` and live relocation evidence |
| 2026-05-25T09:19:38Z worktree-preflight skill allowed sibling worktree by default | Fixed by guidance/tests. Worktree-preflight now prefers repo-local ignored `.worktrees/<slug>` unless an operator or project policy supplies another path. | `internal/tmpl/templates/skills/worktree-preflight/SKILL.md`, `internal/tmpl/templates_test.go` (`TestWorktreePreflightDocumentsRepoLocalDefaultPath`) |
| 2026-05-25T09:19:38Z placeholder codebase-map references can satisfy plan-audit too easily | Partially fixed by scaffold-only detection; deferred for plan-audit fail-closed semantics. The runtime now exposes scaffold-only codebase maps as incomplete/advisory, but changing plan-audit freshness policy to fail on cited placeholder context should be handled with a dedicated policy decision and fixture coverage. | Fixed portion: `internal/engine/artifact/codebase_map.go`, `internal/state/stats.go`; deferred portion: plan-audit context policy |
| 2026-05-25T09:19:38Z plan-audit occurred before canonical worktree binding | Deferred with rationale. This is the same early-binding lifecycle issue as the root/worktree artifact-location friction and should be addressed together to avoid two incompatible planning flows. | Deferred follow-up tied to early worktree binding |
| 2026-05-25T09:19:38Z review/artifact edits reroute through broad S1 research/bundle/audit recovery | Deferred with rationale. Targeted stale-evidence routing needs a richer freshness taxonomy for scope, implementation, review, and closeout edits; this batch avoids weakening fail-closed invalidation. | Deferred follow-up |
| 2026-05-25T09:19:38Z root `slipway` skill catalog duplicates full dedicated skill instructions | Deferred with rationale. Catalog thinning/export slimming is a packaging concern that should be handled in the existing host-skill export plan so generated tool surfaces remain consistent across adapters. | Deferred follow-up; see toolgen/catalog surfaces |
| 2026-05-25T09:19:38Z done archive moves to project root and archived `change.yaml` paths point to former worktree bundle | Fixed by code/tests for metadata correctness while preserving project-root archives. Archived artifact paths are rewritten to archive-local files before `change.yaml` is persisted. | `internal/state/lifecycle.go`, `internal/state/lifecycle_test.go` |
