{{- define "command-next-body" -}}
# Slipway Next

Query the next governed host without advancing lifecycle state.

```bash
slipway next --json
```

## Contract
- `next_skill.name` is the authoritative governed-host handoff.
- Derive the skill path from the local tool convention: `slipway-{name}/SKILL.md`.
- For worktree-bound changes, the code workspace may not contain the exported
  skill catalog. Resolve `next_skill.name` from the source checkout or globally
  installed skill catalog for your tool, then write evidence to
  `next_skill.verification_dir`.
- Treat the default JSON as an action contract, not a context dump.
- Treat `next_skill.skill_constraints` and `next_skill.technique_hints` as action metadata for that host.
- Use only paths returned under `input_context`.
- Do not expect inline policy-pack prose; read policy-pack content only by path when diagnostics or local config provide one.
- Branch on `confirmation_requirement.next_action_kind` (`skill_handoff` | `review_batch` | `preset_confirmation` | `command` | `blocker_resolution` | `confirmation` | `none`) and run `confirmation_requirement.next_command` when present; `next_action` is human prose, not a contract to parse.
- `next` is query-only: it reflects config-level `execution.auto`, has no `--auto`/`--no-auto` flags, and never mutates pending preset confirmations; per-run `slipway run --auto` behavior is on `run`.
- Under auto, non-sensitive skill handoffs and review batches may be reported as `evidence_continuation` with prior authorization sufficient. That is standing authorization for host work, not a loop advance: run/stage loops still stop for the host to run the skill or review and record evidence.
- Auto hard-stops at `security-review` boundaries, sensitive/guardrail confirmations, the intake Approved Summary, done finalization, and evidence gates.
- Write evidence to `next_skill.verification_dir`.
- If `blockers` is non-empty, resolve the blockers before executing the skill.
- If governance control details are needed, run `slipway health --governance --json --change <slug>`.
- Do not infer or guess a governed host without `slipway next --json`.

## Execute
If `next_skill` is present:
1. Read the generated `SKILL.md` for `next_skill.name`.
2. Follow that skill's evidence contract.
3. Run `slipway run --json` when evidence is ready.

If `next_skill` is null, inspect `blockers` for the terminal or remediation action.

## Flags
- `--json`: JSON output
- `--change <slug>`: target a specific active change
- `--diagnostics`: include governance/readiness diagnostic fields
- `--no-auto-pass`: skip auto-pass and report skill eligibility instead
{{- end -}}
