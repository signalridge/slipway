{{define "hard-gate"}}<HARD-GATE>Wait for explicit user confirmation before advancing. Do not call an advancing command (`slipway run` or the explicit stage command) until the user approves; `slipway next` is read-only preview and never advances.</HARD-GATE>{{end}}
