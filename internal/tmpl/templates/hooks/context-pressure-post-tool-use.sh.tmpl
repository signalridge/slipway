#!/bin/sh

# Slipway PostToolUse launcher for {{.ToolID}}.
# Behavior lives in `slipway hook context-pressure`; this file only dispatches.

command -v slipway >/dev/null 2>&1 || exit 0
slipway hook context-pressure 2>/dev/null || true
exit 0
