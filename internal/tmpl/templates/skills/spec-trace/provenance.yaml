sources:
  - source: spec-kitty/spec-kitty-mission-review
    absorbed_as: standalone
    extracted:
      - "bidirectional trace discipline (spec↔code)"
      - "per-item citation requirement"
    dropped:
      - "spec-kitty tool-specific manifest format"
    conflicts_with: []
  - source: trailofbits/spec-to-code-compliance
    absorbed_as: standalone
    extracted:
      - "re-derived acceptance signal check"
      - "scope-drift flagging for unlinked hunks"
    dropped:
      - "engagement-specific deliverable templates"
    conflicts_with: []
