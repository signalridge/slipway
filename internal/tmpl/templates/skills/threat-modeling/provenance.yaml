sources:
  - source: openai/security-threat-model
    absorbed_as: standalone
    extracted:
      - "asset-first enumeration before threats"
      - "STRIDE category requirement per threat"
    dropped:
      - "openai-product narrative framing"
    conflicts_with: []
  - source: openai/security-ownership-map
    absorbed_as: standalone
    extracted:
      - "ownership map: which surface catches regressions of each mitigation"
      - "explicit residual-risk acceptance"
    dropped:
      - "org-chart-specific examples"
    conflicts_with: []
