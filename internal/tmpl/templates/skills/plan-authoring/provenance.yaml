sources:
  - source: superpowers/writing-plans
    absorbed_as: standalone
    extracted:
      - "split by bounded outcome, not file"
      - "each task ends in a testable signal"
      - "sequence deliveries into reviewable PRs"
    dropped:
      - "source-specific narrative tone"
  - source: wshobson/workflow-patterns
    absorbed_as: standalone
    extracted:
      - "plan bundle shape and phase checkpoints"
      - "execution-contract metadata per task"
    dropped:
      - "TDD-for-skills process (kept in distiller SOP)"
  - source: superpowers/executing-plans
    absorbed_as: posture-only
    extracted:
      - "execution-contract posture for task-level claims"
    dropped: []
