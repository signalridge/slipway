sources:
  - source: spec-kitty/spec-kitty-git-workflow
    absorbed_as: standalone
    extracted:
      - "snapshot-before-destructive-op rule"
      - "entanglement classification: dirty / diverged / detached / failed-rebase"
    dropped:
      - "spec-kitty-specific tool hooks"
    conflicts_with: []
  - source: wshobson/git-advanced-workflows
    absorbed_as: standalone
    extracted:
      - "rebase-on-top over force-reset default"
      - "post-recovery diff-vs-snapshot verification"
    dropped:
      - "per-tool worktree recipes"
    conflicts_with: []
  - source: wshobson/block-no-verify-hook
    absorbed_as: standalone
    extracted:
      - "never --no-verify without explicit authorization"
      - "investigate hook failure root cause"
    dropped:
      - "hook-implementation-specific examples"
    conflicts_with: []
