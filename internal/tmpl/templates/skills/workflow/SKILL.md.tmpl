---
skill_id: workflow
name: {{.PublicName}}
description: "Use when starting any non-trivial change in a Slipway-governed repo — adding a feature, fixing a bug, refactoring, schema or auth work, or any multi-step edit — to drive the work through Slipway's governed lifecycle instead of editing ad hoc; also for first contact with the repo, deciding whether to create or continue a governed change, and routing to the right Slipway lifecycle command. Triggers on starting governed work, continuing an active change, or uncertainty about which Slipway surface owns the next step. Skip for one-off trivial edits (a single-line typo, comment, or formatting fix) that need no governed lifecycle."
tool: "{{.ToolID}}"
---

# Slipway Workflow

This skill routes *into* the CLI; it never replaces it. Every lifecycle
transition, gate, and command semantic lives in the CLI, so this entry skill only
decides which command or governed host owns the next step and then hands off.

## Purpose
Use `{{.PublicName}}` as the single exported entry skill for Slipway. It tells
an AI how to enter governed work, how to continue an active change, and when to
hand off to a more specific command surface or governed host skill.

Slipway is a governance CLI for AI-assisted software delivery. A governed
change is the unit of work. `done-ready` means the change has satisfied the
workflow gates and can be finalized; `done` happens only after explicit `slipway done`.

## Layered Surfaces (use the most specific one that fits)
Slipway exposes three AI-facing layers; this skill is only the top entry:
- **This entry skill (`{{.PublicName}}`)** — first contact and routing: decide
  create-vs-continue and pick the right command, then leave as soon as a command
  or governed host owns the step.
- **Command surfaces (`slipway:<command>`)** — one per CLI command with the exact
  arguments and contract; prefer these over re-deriving a command here.
- **Governed host skills (`slipway-<name>`)** — the stage skills the CLI selects
  via `slipway next` or the explicit stage commands (intake, research,
  plan-audit, wave-orchestration, review, …); never guess these, load the one
  the CLI returns.

## Primary Route Table
| Situation | Command |
|---|---|
| Unsure whether a governed change already exists | `slipway status --json` |
| No active change and governed work should begin | `slipway new --json` |
| Active change and you need the next governed host without advancing | `slipway next --json` |
| Active change at S0_INTAKE | `slipway intake --json` |
| Active change at S1_PLAN | `slipway plan --json` |
| Active change at S2_IMPLEMENT | `slipway implement --json` |
| Active change at S3_REVIEW | `slipway review --json` |
| S3 reviewer feedback needs a code/artifact fix | `slipway fix --json` |
| Active change and you want an auto-driver shortcut | `slipway run --json` |
| The change is already `done-ready` and should be finalized | `slipway done` |

## Entry Rules
When starting governed work and you can classify intent directly, enter through
`slipway new --json`.

These are JSON stdin fields for `slipway new --json`, not command-line flags.
Provide them when known:
- `guardrail_domain`
- `complexity`: `trivial`, `simple`, `complex`, or `critical`
- `needs_discovery`

Minimal explicit-classification example:
```bash
echo '{"description":"fix typo","guardrail_domain":"","needs_discovery":false,"complexity":"simple"}' | slipway new --json
```

If these fields are omitted, the CLI applies conservative fallback defaults.

## Runtime Authority Boundaries
- `ResolveNextSkill` selects governed host skills. This skill does not guess or override that selection.
- `commandRegistry` owns command descriptions, arguments, prerequisites, and surface metadata.
- `references/skill-index.md` is informational only. It helps inspect exported host skills; the kernel does not read it back.
- Existing CLI commands remain the only execution surface. This skill never reimplements transitions, gates, or command semantics.

## Handoff After `slipway next --json`
- read the generated SKILL.md for `next_skill.name`
- derive the SKILL.md path from your own tool convention using `slipway-{name}`
- treat the default JSON as an action contract; do not expect diagnostic context fields
- `context_budget` appears only when the guard action is `warn` or `stop`
- if governance controls, policy prose, or full readiness details are needed, run `slipway health --governance --json --change <slug>` or rerun `next` / the current stage command with `--diagnostics`
- read policy packs by path; do not expect inline policy-pack prose in default JSON
- do not look for retired agent fields or a second exported prompt file
- do not guess the next governed host without the CLI response

## Runtime Session Handoff
Use `.git/slipway/runtime/changes/<slug>/handoff.md` only as advisory
continuation notes for a fresh session on an active governed change. Resolve
`<slug>` from fresh `slipway status --json` / `slipway next --json`, not from
the handoff body. Write or refresh it when context is tight, when a checkpoint
or review handoff will split work across sessions, or before stopping with
useful continuation context.

Author it as a compact narrative:
- current position: change slug, lifecycle state/substep, active task or review,
  current blocker or next action, and the command output timestamp if relevant
- session work completed: the material code, artifact, test, or investigation
  changes made in this session
- next-session focus: the smallest useful next action, open decisions, and any
  risks or blockers the next agent should check first
- path references: point to intent, requirements, tasks, decisions, diffs,
  verification, evidence, and logs by path; do not paste or fork those contents
- suggested next skills: derive them from fresh `slipway next --json` fields
  such as `next_skill.name`, `next_skill.verification_dir`,
  `next_skill.selected_review_skills`, and `confirmation_requirement.*`; do not
  infer a governed host from the handoff body
- redaction: remove secrets, credentials, tokens, private keys, and personally
  identifiable information before writing

The per-change `handoff.md` is not lifecycle authority, governed evidence,
freshness input, or a gate. A fresh session must still run
`slipway status --json` and `slipway next --json`, obey lifecycle gates, and
rely on CLI-owned freshness and evidence checks before advancing.

## Delegation Surface
Detailed command contracts live in `references/command-reference.md`. For
commands that ship generated command surfaces, prefer those surfaces over
re-explaining the command inline here.

{{if eq .ToolID "codex"}}For Codex, command surfaces are generated skills under
`.codex/skills/slipway-<command>/SKILL.md`, invoked as `$slipway-<command>` (or via `/skills`).{{else}}For `{{.ToolID}}`, command surfaces live under the generated workspace command
directory `{{.CommandsDir}}`.{{end}}

Every workflow command listed in `references/command-reference.md` ships a
command surface, so you never need to fall back to guessing one for those
commands — including discovery (`codebase-map`), situational execution helpers
such as `evidence`, diagnostics (`learn`, `stats`, `health`, `instructions`),
and setup (`init`). CLI-only helper namespaces such as `slipway tool` are
intentionally not generated as command surfaces; generated skills invoke helpers
directly with `slipway tool ...`.

## Skill Index and Host Handoff
- when exported skill navigation is needed, read `{{.SkillIndexPath}}`
- use the index as reference only; it is not progression authority
- public focus aliases listed in the index are command selectors, not host-skill paths
- load a focus-backed support skill only through the command/focus surface unless an exported host `SKILL.md` path exists
- when governed execution is needed, read the host returned by `slipway next --json`
- derive the host skill path from `next_skill.name` using `.codex/skills/slipway-<name>/SKILL.md` or the equivalent local tool convention
- use technique skills as overlays for posture and procedure, not as progression authorities
- once a more specific host, command surface, or technique skill clearly owns the task, leave this entry skill and follow that more specific surface

## Hard Rules
- do not bypass lifecycle gates
- do not treat this workflow skill as progression authority
- do not treat `.git/slipway/runtime/changes/<slug>/handoff.md` as lifecycle authority,
  governed evidence, freshness input, or a gate
- do not invent command semantics not present in the CLI
- do not guess the next governed host without `slipway next --json`
- do not duplicate long per-command prose here when the reference file or command surface already owns it
