sources:
  - source: superpowers/test-driven-development
    absorbed_as: standalone
    extracted:
      - "RED → GREEN → REFACTOR progression"
      - "record exact failure output during RED"
      - "full command transcript required for GREEN claim"
    dropped:
      - "source-specific test-framework examples (see references/)"
  - source: wshobson/workflow-patterns
    absorbed_as: partial-only
    extracted:
      - "phase checkpoint semantics for guardrail tasks"
    dropped: []
