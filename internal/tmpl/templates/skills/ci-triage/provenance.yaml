sources:
  - source: openai/gh-fix-ci
    absorbed_as: standalone
    extracted:
      - "classify-before-retry discipline"
      - "failure categories: code / test / infra / flake / env-drift"
    dropped:
      - "gh-cli-specific invocations"
    conflicts_with: []
  - source: getsentry/iterate-pr
    absorbed_as: partial-only
    extracted:
      - "infra-fault retry-with-audit posture"
    dropped:
      - "sentry-PR-workflow-specific steps (kept in review-comment-triage partial)"
    conflicts_with: []
