sources:
  - source: spec-kitty/spec-kitty-implement-review
    absorbed_as: standalone
    extracted:
      - "one-batch dispatch with reviewable manifest"
      - "schema validation at rejoin"
    dropped:
      - "tool-specific CLI invocations"
    conflicts_with: []
  - source: superpowers/dispatching-parallel-agents
    absorbed_as: standalone
    extracted:
      - "disjoint partition rule"
      - "per-executor brief template"
    dropped:
      - "worked examples specific to their agent runtime"
    conflicts_with: []
  - source: superpowers/subagent-driven-development
    absorbed_as: standalone
    extracted:
      - "orchestrator retains verdict authority"
      - "rejection criteria authored up-front"
    dropped:
      - "long narrative case studies"
    conflicts_with: []
